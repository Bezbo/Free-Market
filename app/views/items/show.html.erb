<p id="notice"><%= notice %></p>

<%= debug @item %>

<p>
  <b>Name:</b>
  <%= @item.name %>
</p>

<p>
  <b>Discription:</b>
  <%= @item.discription %>
</p>

<p>
  <b>added by:</b>
  <%= @item.user.email %>
</p>

<p>
Tags: <%= raw @item.tags.map(&:name).map { |t| link_to t, tag_path(t) }.join(', ') %>
</p>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>